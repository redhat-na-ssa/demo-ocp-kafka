apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
- includeSelectors: true
  pairs:
    app: kafka

resources:
  - kafka-cluster.yaml
  - kafka-metrics-config.yaml
  - kafka-nodepool.yaml
