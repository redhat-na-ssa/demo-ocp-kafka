apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaMirrorMaker2
metadata:
  labels:
    app: my-mm2-cluster
  name: my-mm2-cluster
spec:
  version: 4.0.0
  rack:
    topologyKey: topology.kubernetes.io/zone
  replicas: 1
  connectCluster: "my-cluster-target"
  clusters:
    - alias: "my-cluster-source"
      bootstrapServers: my-cluster-kafka-bootstrap-strimzi.apps.demo.sandbox1495.opentlc.com:443
      tls:
        trustedCertificates:
          - secretName: "mirror-maker-source-cluster-certs"
            certificate: "source-kafka-cluster.crt"
      authentication:
        type: tls
        certificateAndKey:
          secretName: "mirror-maker-source-cluster-certs"
          certificate: "mm-user.crt"
          key: "mm-user.key"
    - alias: "my-cluster-target"
      bootstrapServers: my-cluster-kafka-bootstrap:9093
      tls:
        trustedCertificates:
          - secretName: "my-cluster-cluster-ca-cert"
            certificate: "ca.crt"
      authentication:
        type: tls
        certificateAndKey:
          secretName: "mm-user"
          certificate: "user.crt"
          key: "user.key"
      config:
        # -1 means it will use the default replication factor configured in the broker
        config.storage.replication.factor: -1
        offset.storage.replication.factor: -1
        status.storage.replication.factor: -1
  mirrors:
    - sourceCluster: "my-cluster-source"
      targetCluster: "my-cluster-target"
      sourceConnector:
        config:
          # -1 means it will use the default replication factor configured in the broker
          replication.factor: -1
          offset-syncs.topic.replication.factor: -1
          sync.topic.acls.enabled: "false"
          replication.policy.class: "org.apache.kafka.connect.mirror.IdentityReplicationPolicy"
      heartbeatConnector:
        config:
          # -1 means it will use the default replication factor configured in the broker
          heartbeats.topic.replication.factor: -1
          replication.policy.class: "org.apache.kafka.connect.mirror.IdentityReplicationPolicy"
      checkpointConnector:
        config:
          # -1 means it will use the default replication factor configured in the broker
          checkpoints.topic.replication.factor: -1
          replication.policy.class: "org.apache.kafka.connect.mirror.IdentityReplicationPolicy"
      topicsPattern: ".*"
      groupsPattern: ".*"
  metricsConfig:
    type: jmxPrometheusExporter
    valueFrom:
      configMapKeyRef:
        name: mirror-maker-metrics-config
        key: config-yaml
