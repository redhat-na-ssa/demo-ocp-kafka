apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# labels:
# - includeSelectors: false
#   pairs:
#     app: streams

resources:
  - prometheus-additional-scrape-secret.yaml
  - prometheus-streams-pod-monitor.yaml
  - prometheus.yaml
