apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

labels:
  - includeSelectors: true
    pairs:
      app: kafka

resources:
  - users.yaml

patches:
  - target:
      kind: Kafka
    patch: |-
      - op: add
        path: /spec/kafka/authorization/type
        value: simple
